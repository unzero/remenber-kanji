#ifndef CONNECTION_H
#define CONNECTION_H

/*
*HEADERS FOR THE CLASS
*/
#include<string> 
//#include "Driver/sqlite3.h"

class Connection{
	public:
		//Connection(string name_tmp);
		//bool connect();
		bool lol();
	private:
	//	string name;
//		sqlite3 *db_file;
		bool is_open;
};
#endif
